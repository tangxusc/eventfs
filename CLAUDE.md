# 开发规范

**核心原则**：证据优先，用户指令 > 本规范 > 默认行为。

**语言与注释**  
- 中文回复；中文注释简短，解释意图和“为什么”，必要时注明边界。  
- 公共接口必须包含文档（功能、参数、返回、异常）。  
- 避免废话，修改代码同步更新注释；用 `TODO`/`FIXME` 标记待办。

**开发流程**  
- Clean Code +  e2e 和模糊测试。  
- 实施前必须：≥3 个澄清问题、探查≥3 个相关文件、提供≥2 个方案对比、用户书面确认、设计自检存档。  
- 复杂任务逐步推进，禁止简化实现。依赖自动安装。

**测试**  
- 行覆盖 ≥80%，分支覆盖 ≥80%，默认 e2e。  
- 测试失败先修复，不提交未验证改动。

**编译（Rust）**  
- 默认 debug，编译后清理中间产物。

**调试流程**  
- 观察错误 → 假设根因 → 验证 → 修复 → 测试通过 → 提供证据。

**暂停条件**（立即询问）  
- 设计假设不确定、复用不确定、依赖冲突、指令歧义。

**禁止行为**  
- 不验证假设即修、不测试即称完成、不探查即提方案、说“应该能”、完成声明无证据。

**文档同步**  
- 代码改动同步更新设计文档/README。